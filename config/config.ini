[server]
# Hostname für TLS-Zertifikat
hostname = localhost

# Port (Standard: 1965)
port = 1965

# Zertifikat-Verzeichnis (automatische Generierung)
certs = /srv/gemini/certs

[static]
# Root-Verzeichnis für statische Dateien
root = /srv/gemini/content

# Index-Datei
index = index.gmi
